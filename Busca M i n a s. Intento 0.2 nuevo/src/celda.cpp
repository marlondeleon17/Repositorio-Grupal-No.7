#include "Celda.h" // Constructor de la clase Celda

// Inicializa una nueva celda con los siguientes valores predeterminados:
// - mina: false (indica que la celda no contiene una mina inicialmente)
// - descubierta: false (indica que la celda no ha sido revelada aún)
Celda::Celda() : mina(false), descubierta(false) {}
